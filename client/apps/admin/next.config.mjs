import { nextSharedConfig } from "@repo/config/next.config.mjs";

/* eslint-disable */
// @ts-nocheck

/** @type {import('next').NextConfig} */
const nextConfig = nextSharedConfig();

export default nextConfig;
