import { Card, CardContent, CardHeader } from "@jamsr-ui/react";

export const ProductImages = () => {
  return (
    <Card>
      <CardHeader heading="Product Images" />
      <CardContent>ProductImages</CardContent>
    </Card>
  );
};
