import { Button } from "@jamsr-ui/react";

export const PayBtn = () => {
  return (
    <div>
      <Button
        fullWidth
        size="lg"
        color="primary"
      >
        Complete Payment
      </Button>
    </div>
  );
};
